<!DOCTYPE html>
<html>
<head>
  <title>Edit Person</title>
  <style>
    form { max-width: 500px; margin: 0 auto; }
    .form-group { margin-bottom: 15px; }
    label { display: block; margin-bottom: 5px; }
    input, select { width: 100%; padding: 8px; }
    .btn { padding: 10px 15px; border: none; border-radius: 4px; cursor: pointer; }
    .btn-primary { background-color: #007bff; color: white; }
  </style>
</head>
<body>
  <h1>Edit Person</h1>
  <form action="/person/<%= person._id %>?_method=PUT" method="POST">
    <div class="form-group">
      <label for="name">Name:</label>
      <input type="text" id="name" name="name" value="<%= person.name %>" required>
    </div>
    <div class="form-group">
      <label for="age">Age:</label>
      <input type="number" id="age" name="age" value="<%= person.age %>" required min="0">
    </div>
    <div class="form-group">
      <label for="gender">Gender:</label>
      <select id="gender" name="gender" required>
        <option value="Male" <%= person.gender === 'Male' ? 'selected' : '' %>>Male</option>
        <option value="Female" <%= person.gender === 'Female' ? 'selected' : '' %>>Female</option>
        <option value="Other" <%= person.gender === 'Other' ? 'selected' : '' %>>Other</option>
      </select>
    </div>
    <div class="form-group">
      <label for="mobileNumber">Mobile Number:</label>
      <input type="tel" id="mobileNumber" name="mobileNumber" value="<%= person.mobileNumber %>" required pattern="[0-9]{10}">
    </div>
    <button type="submit" class="btn btn-primary">Update Person</button>
  </form>
</body>
</html>